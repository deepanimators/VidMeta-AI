# Optional frame enrichment dependencies
# Install with: pip install -r requirements-optional.txt
# The service degrades gracefully if these are absent.
#
# scenedetect: better scene-cut detection
# ultralytics: YOLO object detection per frame (~700MB PyTorch)
# easyocr: OCR text extraction from frames
# moondream: moondream2 frame captioning (~900MB model on first use)
scenedetect>=0.6.4
ultralytics>=8.3.0
easyocr>=1.7.0
moondream>=1.1.0
