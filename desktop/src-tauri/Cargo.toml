[package]
name = "vidmeta-ai"
version = "2.0.0"
description = "VidMeta AI desktop shell"
authors = ["VidMeta AI contributors"]
license = "MIT"
edition = "2021"

[lib]
name = "vidmeta_ai_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
