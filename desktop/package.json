{
  "name": "vidmeta-ai-desktop",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac": "APPLE_SIGNING_IDENTITY='Developer ID Application: Deepak Mohan (B3L935WNUC)' tauri build --bundles app,dmg",
    "build:windows": "tauri build --bundles nsis,msi",
    "build:linux": "tauri build --bundles deb,rpm,appimage"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  }
}
