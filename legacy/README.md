# Legacy Streamlit App

`app_streamlit.py` is retained only as a migration reference. The supported runtime is now the FastAPI service in `api/` plus the React/Vite dashboard in `web/`.

The runtime dependency list no longer installs Streamlit by default.
